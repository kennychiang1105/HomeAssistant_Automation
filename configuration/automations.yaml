# UI reference automations (not loaded)
# Primary runtime source is configuration/Automations via include_dir_merge_list.
# Kept intentionally empty to avoid duplicate loading with AI-managed automations.
[]
